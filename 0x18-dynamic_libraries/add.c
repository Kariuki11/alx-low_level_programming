#include <stdio.h>
#include <stdlib.h>

/**
 * add - a func that adds two numbers
 * @a: first perimeter
 * @b: second perimeter
 * Return: addition of two numbers
 */
void add(int a, int b)
{
	return (a + b);
}
