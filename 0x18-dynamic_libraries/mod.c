#include <stdio.h>
#include <stdlib.h>

/**
 * mod - returns remainder of two numbers
 * @a: first perimeter
 * @b: second perimeter
 * Return: remainder of two numbers
 */
void mod(int a, int b)
{
	return (a % b);
}
