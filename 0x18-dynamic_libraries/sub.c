#include <stdio.h>
#include <stdlib.h>

/**
 * sub - a func that subtracts two numbers
 * @a: first perimeter
 * @b: second perimeter
 * Return: subtraction of two numbers
 */
void sub(int a, int b)
{
	return (a - b);
}
