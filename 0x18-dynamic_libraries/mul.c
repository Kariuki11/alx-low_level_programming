#include <stdio.h>
#include <stdlib.h>

/**
 * mul - a func multies two numbers
 * @a: first perimeter
 * @b: second perimeter
 * Return: multiplies of two numbers
 *
void mul(int a, int b)
{
	return (a * b);
}
